# Recce backend — Python 3.11+
# Story-agent pipeline (LangGraph) with all model calls routed through fal.

fastapi
uvicorn[standard]
websockets
python-dotenv

# Agent orchestration (LangGraph only — no langchain-anthropic; fal replaces it)
langgraph

# Model access via fal: Claude Sonnet 4.6 text (openrouter/router) and
# gpt-image-2 image generation (fal-ai/gpt-image-2) for the visual agents.
fal-client

# Persistence: Supabase (async client) + its Postgrest dependency
supabase
postgrest
